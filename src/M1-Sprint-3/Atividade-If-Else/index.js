// Kata 1

/* let usuarioAutenticado = true;
if (usuarioAutenticado == true){
    console.log('Seja bem vindo')
}; */

// Kata 2

/* let usuario = 'Ana';
let autenticado = false
if (usuario == 'Ana' && autenticado == false){
    console.log(`Olá, ${usuario}. Você está autenticado.`)
}; */

// Kata 3

/* let produto = 'maçã';
let quantidadeDoProduto = 2;
let quantidadeFaltandoParaPromocao = 5 - quantidadeDoProduto;
if (produto == 'maçã' && quantidadeDoProduto >= 5){
    console.log(`Você recebeu uma promoção, o valor do produro será R$5.00.`)
}
else {
    console.log(`Se você adicionar mais ${5 - quantidadeDoProduto}, o valor sairá por R$5.00.`)
}; */