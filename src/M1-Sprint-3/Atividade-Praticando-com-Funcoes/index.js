// Kata 1

/* function imprimirNumero (a) {
    let numeroInteiro = a 
    console.log(numeroInteiro);
}

imprimirNumero (10); */

// Kata 2

/* function somaNumeros (a, b, c) {
    console.log(a + b + c);
}
somaNumeros(10, 5, 5); */

// Kata 3

/* function mediaAnual (a, b) {
    let media = (a + b) / 12
    console.log(media.toFixed(2));
}

mediaAnual (15, 20); */

// Kata 4

/* function divisaoPorDois (a, b) {
    let divisao = a % b;
    if (divisao == 0) {
        alert(`Esta divisão é exata`)
    } else {
        alert(`Esta é uma divisão inexata e o resto é ${divisao}`)
    }
}

divisaoPorDois (11, 2); */

// Kata 5

/* function seuQuadrado (a) {
    let quadrado = a * a;
    alert(`O quadrado de ${a} é ${quadrado}`);
}

seuQuadrado (2); */

// Kata 6

/* function mediaPonderada (a, b, c, d) {
    let media = (( a * 1) + (b * 2) + (c * 3) + (d * 4)) / 10;
    alert(`A média ponderada é ${media}`);
}

mediaPonderada (1, 2, 3, 4); */

// Kata 7

/* function mediaPonderada (m, p, c, i){
    let media = ((m * 2) + p + c + i) / 5;
    alert(`A média ponderada é de ${media}`)
}

mediaPonderada (7, 5, 10, 8); */

// Kata 8

/* function saldoAplicacao (a) {
    let saldo = a * 0.03 + a;
    alert(`O novo salário é de R$${saldo.toFixed(2)}`);
}
saldoAplicacao (1100.00); */

// Kata 9

/* function areaTriangulo (b, h){
    let areaB = parseFloat(prompt(`Digite a base do triângulo`));
    let areaH = parseFloat(prompt(`Digite a altura do triângulo`));    
    let area = (b * h)/ 2;
    alert(`A área do triângulo é ${area}`);
}

areaTriangulo (10, 6); */